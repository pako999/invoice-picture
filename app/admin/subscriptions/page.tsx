import { redirect } from "next/navigation";import{isCurrentUserAdmin}from"@/lib/admin";import{getAdminDashboardData}from"@/lib/admin-dashboard";import{AdminDashboard}from"./dashboard";import{isPaidPlan,type PaidPlan}from"@/lib/plans";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){if(!(await isCurrentUserAdmin()))redirect("/");const p=await searchParams;const email=typeof p.email==="string"?p.email:"";const raw=typeof p.plan==="string"?p.plan:"basic";const plan:PaidPlan=isPaidPlan(raw)?raw:"basic";const billing=p.billing==="yearly"?"yearly":"monthly";return <AdminDashboard data={await getAdminDashboardData()} initialEmail={email} initialPlan={plan} initialBilling={billing}/>}
